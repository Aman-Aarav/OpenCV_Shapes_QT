Draw Shapes using OpenCV and Qt 

This project demonstrates how to draw basic geometric shapes using OpenCV inside a Qt application. It’s a great example of integrating OpenCV graphics with a Qt-based C++ project.

Features:
  Creates a blank canvas using OpenCV Mat
  Draws multiple geometric shapes: Line, Rectangle (outlined and filled), Circle, Ellipse, Polygon
  Demonstrates use of: cv::line, cv::rectangle, cv::circle, cv::ellipse, cv::polylines, cv::fillPoly
  Displays the output using cv::imshow
